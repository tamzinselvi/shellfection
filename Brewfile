# Homebrew-managed shellfection defaults.
#
# This file is the macOS package source of truth. Keep Linux apt/yum,
# npm, pip, symlinks, clones, and themer configuration in config.json.

brew "wget"
brew "tmux"
brew "reattach-to-user-namespace"

# config.json previously requested `mpv --with-libcaca`; Homebrew Bundle
# installs the supported formula and does not preserve obsolete formula
# build options.
brew "mpv"

brew "neovim"
brew "cmatrix"
brew "the_silver_searcher"
brew "tig"
brew "pandoc"
brew "lynx"
brew "zsh"
brew "python"
brew "fftw"
brew "ncurses"
